Neighborhood Parser

Parse shapefiles containing mulit-polygons into a consise CSV representation.  I've found this useful when working with the Zillow neighborhood datasets (http://www.zillow.com/howto/api/neighborhood-boundaries.htm).
